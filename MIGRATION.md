# Миграция с SQLite на Supabase

## Что изменено

### 1. База данных
- ❌ Удалено: SQLite (`github.com/mattn/go-sqlite3`)
- ✅ Добавлено: Supabase (`github.com/supabase-community/supabase-go`)

### 2. Новые функции
- ✅ Просмотр своих записей (кнопка "📋 Мои записи")
- ✅ Отмена записи за 2 часа до процедуры (T-2)
- ✅ Кнопка "Консультация" с прямым звонком
- ✅ Проверка активности мастеров (поле `active`)

### 3. Файлы
- `db.go` - полностью переписан для Supabase
- `config.go` - добавлены `SUPABASE_URL`, `SUPABASE_KEY`, `CONSULT_PHONE`
- `main.go` - добавлены функции для просмотра и отмены записей
- `schema.sql` - SQL схема для Supabase
- `.env.example` - пример конфигурации
- `README.md` - обновленная документация

## Шаги для запуска

### 1. Настройка Supabase

1. Зайдите на [supabase.com](https://supabase.com)
2. Создайте новый проект
3. Перейдите в SQL Editor
4. Скопируйте содержимое `schema.sql` и выполните
5. Скопируйте:
   - Project URL (Settings → API → Project URL)
   - Anon/Public Key (Settings → API → anon/public)

### 2. Настройка .env

Создайте файл `.env`:

```env
BOT_TOKEN=ваш_токен_бота
SUPABASE_URL=https://ваш-проект.supabase.co
SUPABASE_KEY=ваш_anon_key
ADMINS=ваш_telegram_id
CONSULT_PHONE=+79267640131
```

### 3. Запуск

```bash
# Установка зависимостей
go mod download

# Запуск бота
go run .
```

Или через Docker:

```bash
docker-compose up -d
```

## Структура таблиц Supabase

### masters
```sql
id, name, code, contact, gender, active
```

### packages
```sql
key, name, description, price
```

### slots
```sql
id, date, time, gender, master_id, master_name, 
status, user_id, username, booked_at, source
```

## Проверка работы

1. Запустите бота: `/start`
2. Нажмите "📍 Записаться на Хиджаму"
3. Выберите процедуру → увидите кнопку "Консультация"
4. Пройдите процесс записи
5. Нажмите "📋 Мои записи" → увидите свою запись
6. Если до записи > 2 часов → можно отменить

## Исправленные ошибки

1. ✅ Заменена SQLite на Supabase
2. ✅ Добавлена проверка T-2 часа для отмены
3. ✅ Добавлена кнопка консультации с `tel:` ссылкой
4. ✅ Добавлено поле `active` для мастеров
5. ✅ Убраны зависимости CGO из Dockerfile
6. ✅ Добавлена timezone поддержка
